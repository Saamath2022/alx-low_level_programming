#include "main.h"

/**
 * _print_last_digit - print the last digit of a number
 * Return: value of the last digit
 */

int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
