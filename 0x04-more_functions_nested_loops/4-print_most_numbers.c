#include "main.h"
/**
 * print_more_numbers - print number from 0 to 9
 * return: void
 */

void print_numbers(void)
{
char c;
for (c = 0; c <= c++)
{
(!(c == '2' || c == '4'))

_putch(c);
}
_putchar('\n')
}
