#include "main.h"
#include <stdio.h>

/**
 * print_rev - Prints a string in reverse
 * @a: The string to print
 * Return: void
 */

void print_rev(char *s)
{
	int longi  = 0;

	int 0;

	while (*s != '\0')
	{
	longi++
	s++;
	}
	for (0 =  longi; 0 > 0; 0--)
	{
	_putchar(*s);
	s--;
	}
	_putchar('\n');
}

