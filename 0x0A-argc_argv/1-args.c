#include <stdio.h>
#include "main.h"

/**
 * main - count the number of argms that are passed
 * @argc:	An integer parameters
 * @argv:	An array parameters that are passed
 * Return: Always 0;
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("Number of Arguments: %d\n", argc);
	return (0);
}
